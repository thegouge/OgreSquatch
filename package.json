{
  "name": "ogre-squatch",
  "version": "1.1.0",
  "description": "overwatch stat tracker based on the traversy media tutorial",
  "main": "./server/server.js",
  "scripts": {
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "install:all": "yarn install && yarn install --prefix client && yarn install --prefix server",
    "dev": "concurrently \"yarn server\" \"yarn client\"",
    "build": "yarn install:all",
    "start": "node ./server/server.js",
    "predeploy": "yarn build --prefix client",
    "deploy": "echo 'still working on that'"
  },
  "author": "thegouge <thegouge42@gmail.com> (https://thegouge.github.io)",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.0.1",
    "dotenv": "^16.0.3"
  }
}
